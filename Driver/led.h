/*****************************************************************
  Copyright (c) [2026], [开发者] All rights reserved
  File Name   : led.h
  Description : 板载 LED 驱动（LED1 -> PB5，LED2 -> PB6）
  History     :
         2026-09-13   create
*****************************************************************/

/* ================================= 头文件保护宏 ================================= */
#ifndef __LED_H
#define __LED_H

/* ================================= 依赖头文件包含区 ================================= */
#include "gd32f4xx.h"

/* ================================= 导出宏定义区 ================================= */
/* 板载蓝色 LED 的引脚定义 */
#define LED1_GPIO_PORT      GPIOB
#define LED1_GPIO_PIN       GPIO_PIN_5
#define LED2_GPIO_PORT      GPIOB
#define LED2_GPIO_PIN       GPIO_PIN_6

/* 两个 LED 挂在同一个 GPIO 端口上 */
#define LED_GPIO_CLK        RCU_GPIOB

/* 点亮电平：1 = 高电平点亮（本板 LED 阳极经限流电阻接 IO，阴极接 GND）
               0 = 低电平点亮 */
#define LED_ON_LEVEL        1U

/* ================================= 导出类型定义区 ================================= */

/* ================================= 导出常量定义区 ================================= */

/* ================================= 导出全局变量声明区 ================================= */

/* ================================= 导出函数声明区 ================================= */
/* 初始化 LED 使用的 GPIO（推挽输出，默认熄灭） */
void led_init(void);

/* 点亮 / 熄灭 / 翻转指定 LED */
void led_on(uint32_t gpio_periph, uint32_t pin);
void led_off(uint32_t gpio_periph, uint32_t pin);
void led_toggle(uint32_t gpio_periph, uint32_t pin);

#endif /* __LED_H */

/*****************************************************************  END OF FILE  ***/